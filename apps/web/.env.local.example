# Frontend Environment Variables (Local Development)
# Copy this to .env.local and fill in your actual values

# API URL (must match your running API server)
NEXT_PUBLIC_API_URL=http://localhost:8787
