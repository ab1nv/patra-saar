name = "patrasaar-api"
main = "src/index.ts"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# Document storage via KV (no credit card required, unlike R2)
# Max 25MB per value — we cap user uploads at 15MB
[[kv_namespaces]]
binding = "DOCUMENTS"
id = "YOUR_DOCUMENTS_KV_ID"

# User-uploaded document vectors
[[vectorize]]
binding = "VECTORIZE"
index_name = "patrasaar-chunks"

# Pre-indexed Indian legal corpus (IPC, BNS, SC judgements, etc.)
[[vectorize]]
binding = "LEGAL_CORPUS"
index_name = "patrasaar-legal-corpus"

[[d1_databases]]
binding = "DB"
database_name = "patrasaar-db"
database_id = "YOUR_D1_ID"

[[kv_namespaces]]
binding = "CACHE"
id = "YOUR_KV_ID"

[ai]
binding = "AI"

[vars]
ENVIRONMENT = "development"

[env.production]
vars = { ENVIRONMENT = "production" }
